</div>
<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="jumbotron" id="main-jumbotron">
  <div class="container">
    <div class="row">
      <div class="col-md-7">
        <h1 class="white-text">Open Waters</h1>
         <p class="white-text">Record your fly fishing experiences and share them with the world!</p>
      </div>
      <% if !current_user %>
      <div class="col-md-5">
            <br>
            <%= form_for(:user, url: registration_path(:user)) do |f| %>
              <div class="form-group">
                <%= f.text_field :username, class: 'form-control', placeholder: "Enter Username" %>
              </div>
              <div class="form-group">
                <%= f.email_field :email, class: 'form-control', placeholder: "Enter Email" %>
              </div>
              <div class="form-group">
                <%= f.password_field :password, class: 'form-control', placeholder: "Enter Password" %>
              </div>
              <div class="form-group">
                <%= f.submit "Sign up for TripSharers", class: 'btn btn-primary btn-lg col-md-12' %>
              </div>
              <div class="form-group">
              </div>
            <% end %>
          <% end %>
      </div>
    </div>
  </div>
</div>
<container>
